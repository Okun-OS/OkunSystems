{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && (npx tsx prisma/seed.ts || echo \"WARNUNG: prisma/seed.ts fehlgeschlagen — Start wird fortgesetzt\") && (npx tsx prisma/seed-library.ts || echo \"WARNUNG: prisma/seed-library.ts fehlgeschlagen — Start wird fortgesetzt\") && (npx tsx prisma/seed-questions.ts || echo \"WARNUNG: prisma/seed-questions.ts fehlgeschlagen — Start wird fortgesetzt\") && (npx tsx prisma/seed-closing.ts || echo \"WARNUNG: prisma/seed-closing.ts fehlgeschlagen — Start wird fortgesetzt\") && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
